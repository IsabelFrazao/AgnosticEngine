# Copy to .env.local and adjust. Required for Zod validation in src/env.ts.
NEXT_PUBLIC_API_URL=http://localhost:3000/api
# Optional: server-side remote observability sink for structured log entries.
# AE_LOG_INGEST_URL=https://logs.example.com/ingest
# AE_LOG_INGEST_TOKEN=replace-with-secret-token
