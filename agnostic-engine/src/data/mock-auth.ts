/**
 * Demo-only permission set for local development.
 * Replace this with authenticated user permissions from the API/session.
 */
export const MOCK_CURRENT_USER_PERMISSIONS = ['courses:read', 'courses:write'];
